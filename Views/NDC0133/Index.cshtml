@model IEnumerable<NguyenDangCu133.Models.NDC0133>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NDCname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NDCGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NDCname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NDCGender)
            </td>
            <td>
                <a asp-action="create" asp-route-id="@item.NDCid">create</a> |
                <a asp-action="Read" asp-route-id="@item.NDCid">Read</a> |
                <a asp-action="Update" asp-route-id="@item.NDCid">Update</a> |
                <a asp-action="Delete" asp-route-id="@item.NDCid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
